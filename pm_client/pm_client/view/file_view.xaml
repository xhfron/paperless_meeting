<UserControl x:Class="pm_client.view.file_list_view"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:pm_client.view"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1300">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="61*"/>
            <RowDefinition Height="389*"/>
        </Grid.RowDefinitions>
        <ListBox HorizontalAlignment="Stretch"   Grid.Row="1" VerticalAlignment="Stretch" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Image Source="/res/drawable/note.png"></Image>
                        <TextBlock Text="hi"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBoxItem>
                <Border BorderThickness="0,0,0,2">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Background="{StaticResource sidebar_white}">
                        <Image Source="/res/drawable/note.png" Margin="20,0,0,0"></Image>
                        <TextBlock Text="hi" Style="{StaticResource title_48}" VerticalAlignment="Center"></TextBlock>
                    </StackPanel>
                </Border>
            </ListBoxItem>
        </ListBox>
        <TextBlock HorizontalAlignment="Stretch"  TextWrapping="Wrap" Text="会议文件" VerticalAlignment="Center" Style="{StaticResource title_64}" />

    </Grid>
</UserControl>
